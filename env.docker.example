# Docker Environment Variables
# Copy this file to .env (docker-compose automatically reads .env file)
# Command: copy env.docker.example .env  (Windows)
# Command: cp env.docker.example .env   (Linux)
# Then edit .env and adjust values as needed

# Database Configuration
DB_USER=root
DB_PASSWORD=rootpassword
DB_HOST=db
DB_PORT=3310
DB_NAME=attendance_db

# Backend Configuration
BACKEND_PORT=8081
JWT_SECRET_KEY=your-secret-key-change-in-production-use-strong-random-key

# Frontend Configuration
FRONTEND_PORT=5173

